# Environment Variables for Render.com
# Copy these to your Render dashboard under Environment Variables

# Python version
PYTHON_VERSION=3.11

# FastAPI environment
ENVIRONMENT=production

# Host and Port (Render sets PORT automatically)
HOST=0.0.0.0

# CORS Origins (add your Render domain after deployment)
ALLOWED_ORIGINS=*

# Upload settings
MAX_UPLOAD_SIZE=5242880
UPLOAD_DIR=static/uploads

# Data directory
DATA_DIR=data
